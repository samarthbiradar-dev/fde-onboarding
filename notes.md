# Onboarding Notes

## Week 1 Goals
- Get familiar with the codebase and tooling
- Complete Project 1: Folder Stats CLI
- Set up local dev environment

## Learnings
- Python `os.walk()` recursively traverses directories
- `os.path.getsize()` returns file size in bytes
- Collections `defaultdict` is great for grouping data
